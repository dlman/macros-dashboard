# Macros Dashboard

Personal nutrition, weight, sleep, and training dashboard built as a single-file HTML app.

## Local Use

Open `index.html` in a browser.

## GitHub Pages

This repo is set up to publish the dashboard from the root `index.html` file on the `main` branch.
